# Server
NODE_ENV=development
PORT=10000

# Database
MONGODB_URI=
MONGODB_DATABASE=universal_pharmacy

# Authentication (required in production)
JWT_SECRET=

# Comma-separated browser origins allowed to call the API
# Example: https://your-frontend.example.com
CORS_ORIGIN=
