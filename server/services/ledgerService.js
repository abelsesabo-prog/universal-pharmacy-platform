function fail(message) { throw new Error(message); }
const DECIMAL_SCALE = 100;
export function toMinorUnits(value) { if (typeof value === "bigint") return value; const raw = String(value ?? "").trim(); if (!/^-?\d+(\.\d{1,2})?$/.test(raw)) fail("Money must be an exact decimal with at most two fractional digits."); const negative = raw.startsWith("-"); const clean = negative ? raw.slice(1) : raw; const [whole, fraction = ""] = clean.split("."); const minor = BigInt(whole) * 100n + BigInt((fraction + "00").slice(0, 2)); return negative ? -minor : minor; }
export function fromMinorUnits(value) { const minor = BigInt(value); const negative = minor < 0n; const abs = negative ? -minor : minor; const whole = abs / 100n; const fraction = String(abs % 100n).padStart(2, "0"); return `${negative ? "-" : ""}${whole}.${fraction}`; }
export function assertBalancedLines(lines = []) { if (!Array.isArray(lines) || !lines.length) fail("At least one journal line is required."); let debits = 0n; let credits = 0n; for (const line of lines) { const amount = toMinorUnits(line.amount); if (amount <= 0n) fail("Journal line amount must be positive."); const side = String(line.side || "").toUpperCase(); if (side === "DEBIT") debits += amount; else if (side === "CREDIT") credits += amount; else fail("Journal line side must be DEBIT or CREDIT."); if (!line.account) fail("Journal account is required."); } if (debits !== credits) fail(`Journal is not balanced: debit ${fromMinorUnits(debits)} != credit ${fromMinorUnits(credits)}.`); return { debits, credits }; }
export function buildJournal({ tenantId, currency = "TZS", referenceType, referenceId, description, lines } = {}) { if (!tenantId) fail("Tenant context is required."); if (!currency) fail("Currency is required."); assertBalancedLines(lines); return Object.freeze({ tenantId, currency, referenceType: referenceType || null, referenceId: referenceId || null, description: description || null, lines: lines.map((line) => ({ account: String(line.account), side: String(line.side).toUpperCase(), amountMinor: toMinorUnits(line.amount).toString(), memo: line.memo || null })), immutable: true, createdAt: new Date() }); }
export { DECIMAL_SCALE };
