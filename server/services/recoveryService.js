export const RECOVERY_SCENARIOS = Object.freeze(["DATABASE_OUTAGE", "BAD_DEPLOYMENT", "LOST_DEVICE", "CORRUPTED_LOCAL_STORAGE", "ACCIDENTAL_DELETION"]);
export function recoveryPlan({ scenario, rpoMinutes = 60, rtoMinutes = 120, backupRetentionDays = 30 } = {}) { const s = String(scenario || "").toUpperCase(); if (!RECOVERY_SCENARIOS.includes(s)) throw new Error("Unsupported recovery scenario."); if (![rpoMinutes,rtoMinutes,backupRetentionDays].every(v => Number.isFinite(Number(v)) && Number(v) >= 0)) throw new Error("Recovery targets must be non-negative numbers."); return Object.freeze({ scenario: s, rpoMinutes: Number(rpoMinutes), rtoMinutes: Number(rtoMinutes), backupRetentionDays: Number(backupRetentionDays), drillRequired: true, restoreVerification: ["schema_version", "tenant_isolation", "critical_indexes", "application_health", "audit_integrity"] }); }
export function classifyBackupHealth({ lastBackupAt, now = new Date(), maxAgeMinutes = 1440 } = {}) { const time = new Date(lastBackupAt).getTime(); if (!Number.isFinite(time)) return "MISSING"; return Number(now) - time <= Number(maxAgeMinutes) * 60000 ? "HEALTHY" : "STALE"; }
